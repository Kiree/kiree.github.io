import * as generalUtility from './scripts/util/generalUtility.js';

/**
 * run()
 * Runs the app. What did you expect?
 *
 */
let run = function(){
    generalUtility.printConsoleFooter();
};

run();
